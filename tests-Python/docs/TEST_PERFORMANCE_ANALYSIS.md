# Аналіз продуктивності тестів та рекомендації по оптимізації (Python, legacy)

Цей документ стосується **Python-тестів** (tests-Python). Для продуктивності TypeScript-тестів див. актуальні прогони у [docs/TS_TESTS_RUN_STATUS.md](../../docs/TS_TESTS_RUN_STATUS.md).

---

## 📊 Статистика виконання тестів (Python, login)

### Список тестів з часом виконання:

| № | Назва тесту | Час виконання | Статус |
|---|-------------|---------------|--------|
| 1 | `test_positive_login` | 3.84s | ✅ PASSED |
| 2 | `test_login_with_invalid_password` | 3.07s | ✅ PASSED |
| 3 | `test_login_with_invalid_email` | 6.11s | ✅ PASSED |
| 4 | `test_login_with_nonexistent_user` | 6.75s | ✅ PASSED |
| 5 | `test_login_sql_injection` | **45.97s** | ✅ PASSED |
| 6 | `test_login_with_empty_fields` | 4.65s | ✅ PASSED |
| 7 | `test_login_with_email_only` | 2.37s | ✅ PASSED |
| 8 | `test_login_with_password_only` | 2.58s | ✅ PASSED |

### Загальна статистика:
- **Всього тестів:** 8
- **Загальний час виконання:** 78.64 секунд (1 хвилина 18 секунд)
- **Найповільніший тест:** `test_login_sql_injection` (45.97s - 58% від загального часу)

---

## 🔍 Проблемні зони

1. **Тест SQL Injection (45.97s)** — 12 payloads послідовно, кожен з повним циклом (перехід → заповнення → клік → перевірки).
2. **Очікування** — використання `wait_for_timeout` замість очікування конкретних подій.
3. **Перевірки помилок** — множинні спроби через різні селектори.

---

## 💡 Рекомендації (Python)

- Розбити SQL injection на окремі тести або групи payloads.
- Замінити `page.wait_for_timeout(2000)` на `page.wait_for_load_state("networkidle")` або `page.wait_for_selector(...)`.
- Використовувати один надійний селектор для помилок; фікстури для спільного setup логіну.

Очікувана економія при оптимізації: до ~35–45 с (варіант 2–3 з оригінального документу).

---

*Актуальні тести та метрики — у tests-ts та docs/TS_TESTS_RUN_STATUS.md.*
