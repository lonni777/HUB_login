# Чеклист перед запуском тесту

## ✅ Перевірка перед запуском

### 1. Перевірка конфігурації (.env файл)
Переконайтеся, що файл `.env` містить:
- ✅ `TEST_USER_EMAIL` - ваш реальний email для логіну
- ✅ `TEST_USER_PASSWORD` - ваш реальний пароль
- ✅ `TEST_LOGIN_URL` - URL сторінки логіну (наприклад: `https://hubtest.kasta.ua/user/login`)
- ✅ `TEST_NON_EXISTENT_USER_EMAIL` - email користувача, якого не існує в системі (для негативних тестів)
- ⚠️ `TEST_DASHBOARD_URL` - URL після успішного логіну (опціонально)

### 2. Перевірка залежностей
Всі залежності встановлені:
- ✅ pytest
- ✅ playwright
- ✅ pytest-playwright
- ✅ python-dotenv
- ✅ Браузери Playwright встановлені:
  - ✅ Chromium (за замовчуванням)
  - ✅ Safari (WebKit) - опціонально для кросс-браузерного тестування
  - ✅ Firefox - опціонально для кросс-браузерного тестування

**Встановлення браузерів:**
```bash
# Встановити всі браузери
playwright install

# Або конкретні:
playwright install chromium
playwright install webkit
playwright install firefox
```

### 3. Перевірка структури проекту
- ✅ Файл `tests/test_login.py` існує
- ✅ Файл `config/settings.py` існує
- ✅ Файл `conftest.py` існує
- ✅ Файл `.env` існує та заповнений

## 📋 Що буде виводитись при запуску тесту

### Успішний запуск тесту виглядатиме так:

**На Chromium (за замовчуванням):**
```
============================= test session starts =============================
platform win32 -- Python 3.12.10, pytest-7.4.3, pluggy-1.6.0
cachedir: .pytest_cache
rootdir: D:\HUB_login
configfile: pytest.ini
testpaths: tests
plugins: base-url-2.1.0, playwright-0.4.3
collected 1 item

tests/test_login.py::TestLogin::test_positive_login[chromium] PASSED    [100%]

============================= 1 passed in X.XXs ==============================
```

**На Safari (WebKit):**
```
tests/test_login.py::TestLogin::test_positive_login[webkit] PASSED    [100%]
```

### Якщо є помилки:

**Помилка відсутніх секретів:**
```
ValueError: Відсутні обов'язкові змінні середовища: TEST_USER_EMAIL, TEST_USER_PASSWORD
Створіть файл .env на основі .env.example та заповніть необхідні дані.
```

**Помилка невірного URL:**
```
TimeoutError: page.goto: Timeout XXXXms exceeded
```

**Помилка невірних селекторів:**
```
TimeoutError: locator.fill: Timeout XXXXms exceeded
```

## 🚀 Команди для запуску

### Запуск всіх тестів:
```bash
pytest
```

### Запуск з детальним виводом:
```bash
pytest -v
```

### Запуск конкретного тесту:
```bash
pytest tests/test_login.py::TestLogin::test_positive_login
```

### Запуск з виводом print statements:
```bash
pytest -s
```

### Запуск з відкриттям браузера (headed mode):
```bash
# На Chromium (за замовчуванням)
pytest --headed

# На Safari (WebKit)
pytest --headed --browser webkit

# На Firefox
pytest --headed --browser firefox
```

### Кросс-браузерне тестування:
```bash
# Запуск всіх тестів на Safari (WebKit)
pytest tests/test_login.py --browser webkit -v

# Запуск всіх тестів на Firefox
pytest tests/test_login.py --browser firefox -v

# Запуск на конкретному браузері з відкритим вікном
pytest tests/test_login.py --headed --browser webkit -v
```

## 📝 Що робить тест (покроково)

1. **Завантажує конфігурацію** з `.env` файлу
2. **Відкриває браузер** (Chromium за замовчуванням, або вказаний через `--browser`)
3. **Переходить на сторінку логіну** (URL з `TEST_LOGIN_URL` з `.env` файлу)
4. **Знаходить поле email** за селектором `#email`
5. **Вводить email** з конфігурації (`TEST_USER_EMAIL`)
6. **Знаходить поле паролю** за селектором `#password`
7. **Вводить пароль** з конфігурації (`TEST_USER_PASSWORD`)
8. **Натискає кнопку входу** за селектором `#root-content > div:nth-child(2) > form > button`
9. **Перевіряє успішний логін:**
   - Якщо вказано `TEST_DASHBOARD_URL` - перевіряє зміну URL
   - Якщо не вказано - чекає завантаження сторінки

## ⚠️ Важливі зауваження

1. **Секрети не комітяться в Git** - файл `.env` в `.gitignore`
2. **Тест відкриває реальний браузер** - закрийте зайві вкладки перед запуском
3. **Тест використовує реальні дані** - переконайтеся що обліковий запис активний
4. **Перевірка успішного логіну** - якщо не вказано `TEST_DASHBOARD_URL`, тест просто чекає завантаження сторінки
5. **Кросс-браузерна сумісність** - всі тести працюють однаково на Chromium, Safari (WebKit) та Firefox без змін в коді
