# Звіти про виконання тестів

## 📊 Автоматичне збереження звітів

Проект автоматично генерує звіти після кожного запуску тестів. Всі звіти зберігаються локально і доступні для перегляду в будь-який момент.

## 📁 Структура звітів

### 1. HTML звіт (pytest-html)
**Розташування:** `reports/report_YYYYMMDD_HHMMSS.html` (автоматично генерується з timestamp)

**Що містить:**
- ✅ Статус виконання кожного тесту (PASSED/FAILED/SKIPPED)
- ⏱️ Час виконання кожного тесту
- 📋 Детальна інформація про помилки (traceback)
- 🔍 Метадані про середовище:
  - Версія Python
  - Платформа (ОС)
  - Версії пакетів (pytest, playwright тощо)
  - Список плагінів

**Як переглянути:**
```bash
# Windows - відкрити останній звіт
start reports/report_20260129_170635.html

# Або знайти найновіший файл
# Windows PowerShell
Get-ChildItem reports/*.html | Sort-Object LastWriteTime -Descending | Select-Object -First 1 | ForEach-Object { start $_.FullName }

# Linux/Mac
ls -t reports/*.html | head -1 | xargs open
```

### 2. Bug report для Jira (при падінні тесту)
**Розташування:** `reports/last_failure_bug_report.txt`

**Що містить:**
- Summary для Jira issue
- Опис помилки та кроки відтворення
- Шляхи до скріншота та HTML звіту

**Використання:** при падінні тесту файл оновлюється автоматично. Скопіюйте вміст у Jira для створення баг-репорту.

### 3. Скріншоти при помилках
**Розташування:** `test-results/screenshots/`

**Що містить:**
- 📸 Повноекранні скріншоти сторінки при падінні тестів
- Автоматично зберігаються тільки для тестів, які завершилися з помилкою
- Формат імені: `{test_name}_{timestamp}.png`

**Приклад:**
```
test-results/screenshots/
├── test_positive_login_20250123_143022.png
├── test_login_with_invalid_password_20250123_143045.png
└── ...
```

## ⚙️ Налаштування

### Автоматична генерація звітів

Звіти генеруються автоматично завдяки налаштуванням в `pytest.ini` та хуку `pytest_configure` в `conftest.py`:

**В `pytest.ini`:**
```ini
addopts = -v --tb=short --html=reports/report.html --self-contained-html
```

**В `conftest.py`:**
```python
def pytest_configure(config):
    # Автоматично генерує унікальне ім'я звіту з timestamp
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    report_path = reports_dir / f"report_{timestamp}.html"
    config.option.htmlpath = str(report_path)
```

**Параметри:**
- `--html=reports/report.html` - базовий шлях (автоматично замінюється на версію з timestamp)
- `--self-contained-html` - звіт містить всі необхідні ресурси (CSS, JS) в одному файлі
- **Автоматичний timestamp** - кожен звіт зберігається з унікальним ім'ям: `report_YYYYMMDD_HHMMSS.html`

### Збереження скріншотів

Скріншоти автоматично зберігаються завдяки хуку в `conftest.py`:

```python
@pytest.hookimpl(tryfirst=True, hookwrapper=True)
def pytest_runtest_makereport(item, call):
    # Автоматично зберігає скріншоти при помилках
```

## 📝 Приклади використання

### Запуск тестів з генерацією звіту

```bash
# Запуск всіх тестів (звіт генерується автоматично)
pytest

# Запуск конкретного тесту
pytest tests/test_login.py::TestLogin::test_positive_login

# Запуск з детальним виводом
pytest -v
```

### Перегляд звіту

Після виконання тестів в консолі з'явиться повідомлення:
```
------- Generated html report: file:///D:/HUB_login/reports/report.html -------
```

Відкрийте цей файл в браузері для перегляду детального звіту.

## 🔄 Збереження історії звітів

✅ **Автоматичне збереження історії вже налаштовано!**

Кожен запуск тестів автоматично створює новий звіт з унікальним ім'ям на основі timestamp:
- Формат: `report_YYYYMMDD_HHMMSS.html`
- Приклад: `report_20260129_170635.html`
- Всі звіти зберігаються в папці `reports/`

**Переваги:**
- ✅ Історія всіх запусків зберігається автоматично
- ✅ Легко знайти звіт за датою та часом
- ✅ Можна порівняти результати різних запусків
- ✅ Не потрібно вручну копіювати або перейменовувати файли

**Приклад структури папки reports:**
```
reports/
├── report_20260129_140530.html  # Перший запуск
├── report_20260129_150215.html  # Другий запуск
├── report_20260129_170635.html  # Третій запуск
└── ...
```

## 🗑️ Очищення старих звітів

Звіти не комітяться в Git (додано в `.gitignore`), тому можна безпечно видаляти старі звіти:

```bash
# Видалити всі звіти
rm -rf reports/
rm -rf test-results/

# Або тільки старі скріншоти
rm test-results/screenshots/*.png
```

## 📊 Аналіз звітів

### Що шукати в звіті:

1. **Загальна статистика:**
   - Кількість пройдених/провалених тестів
   - Загальний час виконання

2. **Деталі по кожному тесту:**
   - Час виконання (для виявлення повільних тестів)
   - Повідомлення про помилки (якщо є)

3. **Скріншоти при помилках:**
   - Візуальне підтвердження стану сторінки
   - Допомагає швидко зрозуміти причину падіння

## 🔧 Troubleshooting

### Звіт не генерується

**Проблема:** Після запуску тестів не з'являється повідомлення про генерацію звіту.

**Рішення:**
1. Перевірте, чи встановлено `pytest-html`:
   ```bash
   pip install pytest-html
   ```

2. Перевірте налаштування в `pytest.ini`:
   ```ini
   addopts = ... --html=reports/report.html --self-contained-html
   ```

3. Переконайтеся, що папка `reports/` може бути створена (немає проблем з правами доступу)

### Скріншоти не зберігаються

**Проблема:** При падінні тестів скріншоти не зберігаються.

**Рішення:**
1. Перевірте, чи існує папка `test-results/screenshots/`
2. Перевірте налаштування в `conftest.py` (хук `pytest_runtest_makereport`)
3. Переконайтеся, що тест має доступ до `page` fixture

### Звіт відкривається порожнім

**Проблема:** HTML звіт відкривається, але не відображає дані.

**Рішення:**
1. Переконайтеся, що використовується параметр `--self-contained-html`
2. Спробуйте відкрити звіт в іншому браузері
3. Перевірте консоль браузера на наявність помилок JavaScript
